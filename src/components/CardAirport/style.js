import styled from 'styled-components';

const StyledCard = styled.article`
  background-color: #fafafa;
`;

const ImageWrapper = styled.div`
  width: 200px;
  img {
    display: block;
    width: 100%;
    height: auto;
  }
`;

export { StyledCard, ImageWrapper };
